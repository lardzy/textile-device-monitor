# 10. 纸、纸板和纸浆纤维鉴别分析首版

> 最后更新：2026-08-05
> 当前状态：共享盘读取与完整 DAG 已实现；旧系统真实写入等待 Windows Bridge。

## 1. 流程定义

- 工作流：`纸、纸板和纸浆纤维鉴别分析 GB/T 4688-2020—定性分析`
- slug：`paper-fiber-gbt4688-2020-qualitative`
- 类别：其他
- 原始记录根：
  `08-其他/2022-纸、纸板和纸浆纤维鉴别分析`
- 任务项目名称：`纸、纸板和纸浆纤维鉴别分析`
- 测试方法：`GB/T 4688-2020`

发布流程：

```text
开始
→ 读取纸类定性结果
→ 人工选择一份原始记录
→ 旧系统上传-特种毛-纸纤维定性
→ 旧系统-特纤复核
→ 旧系统-检验记录登记
→ 结束
```

## 2. 读取与推荐规则

1. 文件夹名包含完整检验编号；
2. 候选文件扩展名为 `.xls`，大小写不敏感；
3. 读取 `Sheet1!W32` 为定性结果；
4. 文件索引只保存路径、大小、mtime 和 fingerprint，用户查询不遍历共享盘；
5. 工作簿结果按 fingerprint 缓存，文件变化后重新读取；
6. 任务单项目和方法来自 `execution_task_snapshot_cache`，默认 15 分钟过期，
   合同评审变更可通过强制刷新提前生效；
7. 文件根、文件夹、项目名、测试方法每命中一项加一分，条件越多越靠前。

`W32` 中独立出现数字 `100` 或 `100.0` 时，检验记录登记单位写 `%`；
`1000`、`100.5`、`A100`、`棉100` 不视为独立 100。

## 3. 外部操作字段

### 特种毛上传

- 纤维类别：棉再生纤
- 检验方式：定量
- 检验员：当前执行系统用户在旧系统中的登录人员
- 检验项目：棉再生纤定性
- 份数：1
- 图片子记录：0
- 目标编号：基号、`-1`、`-2`……依次取首个空闲编号
- 上传文件名：`{最终分配编号}-{原始记录文件名}`

### 特纤复核

- 复核对象严格绑定上一节点回执中的主记录；
- 复核项目：棉再生纤定性；
- 不重复复制文件，不生成图片子记录。

### 检验记录登记

- 找到任务单中的纸类项目；
- 实测值写入 `W32` 结果；
- 只有独立 100 时单位写 `%`，否则单位为空；
- 其它登记表头和明细字段保持空；
- 保存后结束，不执行“校对”。

## 4. 当前真实验证

| 编号 | 共享盘候选 | W32 | 单位 | 任务快照 |
| --- | --- | --- | --- | --- |
| `26W006701` | 1 个 `.xls` | 草浆、木浆、竹浆 | 空 | queued |
| `26W006687` | 1 个 `.xls` | 木浆 100 | `%` | queued |

两份原始文件均通过只读解析，源文件未被修改。当前 Docker 中
PostgreSQL、backend、execution-worker 和 frontend 均健康，网页入口为 80 端口。

## 5. Windows 接续顺序

1. 同步当前 Git 工作区到 Windows VM；
2. 重新编译 x86 SpecialWool Writer 与 FinalEntry Writer；
3. 先运行任务单只读 Bridge，完成 `26W006701`、`26W006687` 快照；
4. 核对两个编号均命中项目名称和 `GB/T 4688-2020`；
5. 分别启动运行并选择唯一 `.xls`；
6. 最终批准并执行特种毛上传，核对目标编号、前缀文件名、人员和项目；
7. 执行特纤复核；
8. 执行检验记录登记：701 不带单位，687 带 `%`，均只保存不校对；
9. 保留新增记录供用户在旧系统中人工检查，不删除。

同时继续处理 `260111037`：在重新运行 FinalEntry 前，先用只读探针确认最近一次
失败没有产生第二条登记记录或孤立附件；然后编译包含 CollectData 后权威字段恢复
逻辑的新 Writer，再根据只读结果决定对账或创建新 attempt。
