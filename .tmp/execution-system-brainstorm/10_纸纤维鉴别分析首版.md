# 10. 纸、纸板和纸浆纤维鉴别分析首版

> 最后更新：2026-08-11
> 当前状态：完整 DAG（含判定分支）已在公司内网真实链路验证；
> 上传/复核/登记均经 Windows Bridge 写入旧系统。

## 1. 流程定义

- 工作流：`纸、纸板和纸浆纤维鉴别分析 GB/T 4688-2020—定性分析`
- slug：`paper-fiber-gbt4688-2020-qualitative`
- 类别：其他
- 原始记录根：
  `08-其他/2022-纸、纸板和纸浆纤维鉴别分析`
- 任务项目名称：`纸、纸板和纸浆纤维鉴别分析`
- 测试方法：`GB/T 4688-2020`

发布流程：

```text
开始
→ 读取纸类定性结果
→ 人工选择一份原始记录（仅一份候选时自动提交，零人工干预）
→ 旧系统上传-特种毛-纸纤维定性
→ 旧系统-特纤复核
→ 核对已有登记（单份项目已有记录时选择直接新增或取消）
→ 确认样品识别与判定信息
→ 旧系统-检验记录登记
→ 结束
```

## 2. 读取与推荐规则

1. 文件夹名包含完整检验编号；
2. 候选文件扩展名为 `.xls`，大小写不敏感；
3. 读取 `Sheet1!W32` 为定性结果；同时读取 `Sheet1!M32`
   （判定“标准值与允差”的人工核对数据源之一，缺失不阻断读取）；
4. 文件索引只保存路径、大小、mtime 和 fingerprint，用户查询不遍历共享盘；
5. 工作簿结果按 fingerprint 缓存（profile version 2 起含 M32），
   文件变化后重新读取；
6. 任务单项目和方法来自 `execution_task_snapshot_cache`，默认 15 分钟过期，
   合同评审变更可通过强制刷新提前生效；
7. 文件根、文件夹、项目名、测试方法每命中一项加一分，条件越多越靠前。

`W32` 中独立出现数字 `100` 或 `100.0` 时，检验记录登记单位写 `%`；
`1000`、`100.5`、`A100`、`棉100` 不视为独立 100。

## 3. 录入前确认分支

### 3.1 已有登记

- 上传和复核完成后强制刷新一次旧系统任务快照，以最新 `register_count` 为准；
- `check_count=1` 且 `register_count>0`：暂停并询问“直接新增”或“取消”；
- `check_count>1`：不设登记容量上限，已有登记数达到或超过任务份数时仍自动继续；
- “直接新增”会形成校验和绑定的 `existing_record_decision`，Bridge 与 Writer
  都会复核任务份数、预期已有数量及追加后的数量。

### 3.2 样品识别与判定信息

- 任务单 `sample_identify` 按 `，`、`,`、`、` 拆分并去重；
- 只有一个值时自动填入，但必须由用户确认；超过一个值时提供可编辑候选；
- 候选数量与 `check_count` 不一致时显示警告但不终止；提交值仍必须严格属于
  任务单候选，Writer 联网只读预检还会再次核对当前任务单下拉数据；
- 任务单没有样品识别时，不向旧系统写入该字段。

- 任务单该项目 `give_judgement=0`：节点自动完成，不产生人工任务，
  但存在样品识别时仍会停留供用户选择/确认；登记包中所有判定字段留空；
- `give_judgement=1`：创建人工任务，表单字段：
  - 判定依据：选项取自任务单快照 `check_basis`（顿号/逗号分隔多值）；
  - 判定结果：`符合` / `不符合`；
  - 标准值与允差：必填，**默认预填所选原始记录的 `Sheet1!W32` 结果**
    （与人工登记同文样式一致），人工可按本单实际要求修改；
    表单上方展示两个可拖选/复制/一键填入的数据源：
    任务单项目说明列（快照 `remark`）与所选原始记录的 `Sheet1!M32`。
- 人工登记样式核对（只读查库，2026-08-11）：`260191286`、`260174495`
  两单标准值均与实测值同文（如 `木浆 100`），但存在标准值与原始记录
  不一致的单据，因此标准值必须由人工确认，不再强制同文镜像；
- 登记包判定变体校验（Writer）：判定依据/总评定必填、报告项目名称
  等于项目名、**标准值必填非空（允许与实测值不同）**；未要求判定时
  上述字段必须全部为空。

## 4. 外部操作字段

### 特种毛上传

- 纤维类别：棉再生纤
- 检验方式：定量
- 检验员：当前执行系统用户在旧系统中的登录人员
- 检验项目：棉再生纤定性
- 份数：1
- 图片子记录：0
- 目标编号：基号、`-1`、`-2`……依次取首个空闲编号
  （以写锁内旧系统实况为准，容忍人工增删漂移；详见 CHANGELOG 2026-08-10）
- 上传文件名：`{最终分配编号}-{原始记录文件名}`

### 特纤复核

- 复核对象严格绑定上一节点回执中的主记录；
- 复核项目：棉再生纤定性；
- 不重复复制文件，不生成图片子记录。

### 检验记录登记

- 找到任务单中的纸类项目；
- 顶部“样品识别”写入前一步人工确认的值，并在写后同时核对原始记录表头
  `SampleDescription` 与登记记录 `SampleIdentity`；
- 实测值写入 `W32` 结果；
- 只有独立 100 时单位写 `%`，否则单位为空；
- 要求判定时：判定依据/总评定/报告项目名称/标准值与允差按
  判定确认步骤的人工提交写入；未要求判定时全部留空；
- 除样品识别及上述判定字段外，其它登记表头和明细字段保持原规则；
- 保存后结束，不执行“校对”。

上传、复核和登记的预检通过后均由服务端直接批准并交付 Windows Bridge，
不再依赖运行工作台页面的倒计时或“立即批准”按钮；离开页面不会中断流程。

## 5. 当前真实验证

| 编号 | 共享盘候选 | W32 | 单位 | 判定 | 任务快照 |
| --- | --- | --- | --- | --- | --- |
| `26W006701` | 1 个 `.xls` | 草浆、木浆、竹浆 | 空 | 否 | ready |
| `26W006687` | 1 个 `.xls` | 木浆 100 | `%` | 否 | ready |
| `260174495` | —（人工单） | 木浆 100 | `%` | 是（人工录入参考） | ready |

两份原始文件均通过只读解析，源文件未被修改。当前 Docker 中
PostgreSQL、backend、execution-worker 和 frontend 均健康，网页入口为 80 端口。

## 6. 判定分支相关实现位置

- 读取 M32：`backend/app/execution/paper_fiber.py`
  （`PAPER_FIBER_STANDARD_SOURCE_CELL = "M32"`，随 W32 进入候选
  `result.m32_value` 并顺选择节点输出传递）；
- 判定表单动态生成/自动跳过：`backend/app/execution/engine.py`
  `_paper_judgement_form_schema` / `_auto_complete_paper_judgement`
  （表单生成时从已成功选择节点的 `primary_file.result` 取 W32/M32，
  从 `selected_project.remark` 取说明列，无需修改 DAG 定义）；
- 登记包组装：`backend/app/execution/external_operations.py`
  `prepare_legacy_generic_check_record_entry_operation`
  （`standard_value` 取人工提交值；缺失时报
  `paper_fiber_standard_value_required`；签发受校验和保护的
  `judgement_contract`，供 Bridge 与批准界面共同核对）；
- 存量运行兼容：`backend/app/execution/engine.py`
  `effective_human_task_form_schema` /
  `_reopen_paper_judgement_for_standard_value`
  （旧开放任务动态升级表单；旧判定已完成但登记尚未预检时，先退回
  人工确认再继续，不静默使用 W32）；
- Bridge 校验：`tools/legacy_fibrecheck_bridge/bridge.py`
  `validate_generic_final_entry_machine_payload`
  （新包的判定字段必须逐项等于 `judgement_contract`；旧签发包仍保持
  StandardValue 与 W32 同文的旧约束）；
- Writer 校验：`tools/legacy_fibrecheck_final_entry_writer/src/PackageModel.cs`
  与 `ReadOnlyResolver.cs`（新 FibreCheckFinalEntryWriter.exe SHA256
  `c70baeeba114f7cdd6fa95be3f73d902d3f84d5f1fc6e86e814f7b6167f4934f`，
  已在 Parallels Windows 11 编译并通过 56 项离线自测；仓库打包钉值已同步）；
- 表单数据源 UI：`frontend/src/pages/execution/SchemaFields.jsx`
  （`x-copy-sources`、`x-suggestions`、`x-warning` 渲染）；状态卡片展示
  服务端自动交付进度，不再发起页面批准请求。
