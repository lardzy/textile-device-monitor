# 09. 电镜—纤维微观形貌 GB/T 36422-2018 首版

> 最后更新：2026-08-05
> 状态：已实现“任务匹配 → 选图 → 原始记录生成 → 可选打印确认 → 旧系统上传/复核禁写预检”；旧系统真实写入继续冻结。

## 1. 本轮范围

当前发布流程为：

```text
开始
→ 查找微观形貌图片
→ 人工选择图片结果
→ 准备任务单字段与样品名称候选
→ 人工确认样品名称、样品识别和按需判定
→ 生成并重读核对微观形貌原始记录
→ 选择打印或暂不打印（打印后再人工确认完成）
→ 旧系统上传-特种毛-图片（仅预检）
→ 旧系统-特纤复核（仅预检）
→ 结束
```

旧系统两个节点已具备持久化协议和预检数据结构，但当前三层禁写：能力声明为
不可执行、后端拒绝批准、Bridge/Writer 拒绝领取和执行。尚未完成旧系统子记录
语义及复核联动实机证明前，不得开启真实写入。

## 2. 工作流匹配规则

专用节点类型为 `file.electron_microscopy_gbt36422@1`，数据根为
`electron_microscopy_records/2026-电镜`。推荐分数由以下条件逐项累加：

1. 数据根可用；
2. 顶层文件夹名包含完整检验编号；
3. 同一任务项目的名称属于 `纤维微观形貌` 或 `膜平面形貌`；
4. 同一任务项目的测试方法为 `GB/T 36422-2018`；
5. 用户选择“电镜”为优先类别时再加类别分。

四项业务条件全部命中才是 `full_match`。项目名称与测试方法不能从两个不同
项目拼接。`膜平面形貌` 纳入同标准子项，是 2026-08-03 经真实任务单核验后由
用户明确确认的业务口径。

## 3. 索引与任务单缓存

- 电镜索引由后台递归扫描全部嵌套层级；用户搜索只查询 PostgreSQL 索引，
  不即时遍历共享盘。这样既覆盖现有“编号文件夹/部位/图片”结构，也不会静默
  漏掉未来更深层的项目目录。
- 图片后缀大小写不敏感，支持 BMP、GIF、JPEG/JPG、PNG、TIF/TIFF、WEBP。
- 旧系统任务单使用独立的 `execution_task_snapshot_cache` 缓存；编号统一转大写。
- 任务快照契约为 schema v3；微观形貌项目必须同时携带脱敏的
  `Task_CheckItem` 和 `CheckItem` 标识。旧 v2 或缺少标识的缓存不会暴露
  给新流程，而会立即排队刷新；其它项目仍允许不带该标识。
- 正常快照默认 15 分钟有效；过期时允许先返回旧快照并排队刷新；失败默认
  60 秒后重试。合同评审修改任务信息后，最迟在缓存到期或管理员主动刷新时更新。
- 推荐 API 不直接查询 Oracle。独立 Windows 只读 Bridge 领取待刷新编号，探针
  仅执行 `Task` 和 `Task_CheckItem` 两个参数化 SELECT，确认只读事务后统一回滚。
- Bridge 租约默认 180 秒，单次探针 90 秒超时；领取使用一次性
  `claim_token + bridge_id`，避免重复结果覆盖。

## 4. 图片人工任务

- 先列出全部匹配顶层文件夹，可多选；多选后把图片合并到同一选择界面。
- 至少选 1 张、最多选 10 张；可指定主图。
- 卡片展示缩略图、文件名、来源文件夹和二级部位；支持大图、上一张和下一张。
- 后端重新校验文件夹 ID、图片 ID、候选归属、去重、1～10 上限、索引指纹和
  真实文件状态，不能只依赖前端校验。
- 预览接口只读取电镜根中的已索引图片，并在返回前复核扩展名、大小和指纹。

## 5. 原始记录生成

- 生成器的基础版本模板为
  `gbt36422-2018-microscopy-original-record-v1.xls`，SHA-256 为
  `d2b70e114cb85c89e4961ba2acb1b0cb451656f7a12b5dd5bdbacba1d50f88ba`。
- 正式执行会先按用户已选图片数量绑定旧系统“检验记录登记”返回的
  同名模板，数量无映射时在生成前失败；已确认 `微观形貌.xls`
  专用于 **1 张图片**。当前已建立 1、2、3、5、6、7、10 张的版本化映射，
  4、8、9 张仍按“旧系统无模板”拒绝，不会猜测或复用其它数量的模板。
- 任务快照已升级为 schema v3，分别保留 `sample_names`；样品名称候选使用
  `sample-name-v1`，先保留按业务逗号拆分的完整名称，再追加固定版本 jieba 分词，
  分词不可用时采用确定性回退。
- 样品识别支持中文逗号、英文逗号和顿号；0 项不询问、1 项自动选用、多项由
  用户选择。只有任务项目 `give_judgement` 非空/非 0 时才显示判定依据和判定。
- 写入工作表“微观形貌”：B2 检验编号、B3 样品名称、L3 样品识别；需判否时
  写 B33 和 I34。I33、B34、B35 在本轮保持空白。
- 1～10 张图片在 A4:L32 的 21.6 cm × 11.7 cm 范围内等比例最大化布局。
  单图必须贴满至少一条边并在另一方向居中，不裁剪、不拉伸；多图自适应网格。
- LibreOffice/UNO 保存后重新打开，逐图核对源标识、宽高比、几何位置、
  边界、重叠及默认打印区域 `微观形貌!A1:L37`；单图额外核对最大
  contain 和居中。模板中普通工作表级 `Print_Area` 在保存前移除，仅保留
  Excel 内置打印区域，避免打开时“名称冲突”。
- 方形图片的根因是 LibreOffice 导出旧 BIFF8 `.xls` 时的水平锚点单位与
  Windows Excel 解释不同：源 PNG 仍为 4:3，Excel 曾将图形读成约 1.046:1。
  生成器现对该版本化模板的水平位置和宽度使用 Windows Excel 实测补偿
  `1.2745`，垂直坐标不变；补偿版本进入请求摘要，不会复用旧的方形制品。
- 服务端生成的工作副本保留原模板 basename，文件名为
  `{源编号}-39-8B-纤维形状截面定量试验-2026.xls`。上传到“特种毛检验”时，
  Windows Writer 必须按最终分配编号重新命名为
  `{最终编号}-39-8B-纤维形状截面定量试验-2026.xls`；最终编号包含已分配的
  `-1`、`-2` 等后缀。服务器文件、主记录 `FilePath`、图片子记录
  `PictureFileName/OriginalDataFileName` 和新机器回执必须使用同一个最终文件名。

## 6. 旧系统上传与复核边界

- 上传预检固定业务字段：纤维类别“图片”、检验方式空、检测项目“图片”、份数 1；
  检验员取当前执行系统用户显示名，不接受前端覆盖。
- 上传只接受本次运行、服务端登记且 SHA-256/大小/路径均复核通过的 `.xls` 制品。
- 目标编号按原号、`-1`、`-2` 在本系统围栏内暂定；只读探针已能查询旧系统
  远端占用，但实库没有 `SampleNo` 唯一约束，且已存在同号主记录。未来必须
  由 Bridge 串行化“重查编号族 → 分配 → 保存 → 精确回读”，服务端暂定值不能
  视为最终分配。
- 复核节点只能引用同一运行中已完成并回读核对的图片上传操作，使用独立阶段和
  副作用边界；当前上传不可完成，所以正式流程会停在上传预检，不会提前复核。
- 待实机证明：`OriginalDataPictureFile + CheckItemID` 子记录创建/回读，以及复核
  对主记录、细度/定量子记录和 `CheckItemID` 的联动更新。

### 6.1 禁写闭环补充（2026-08-05）

- 上传预检现已绑定人工确认的 `selected_project_key`、脱敏
  `Task_CheckItem/CheckItem` 标识、项目名称和测试方法；支持的名称为“纤维微观
  形貌”“膜平面形貌”，方法必须精确为 `GB/T 36422-2018`。
- Windows Python 探针增加图片上传专用只读模式，查询编号族、任务项目、图片
  子表、唯一索引和服务器时间；搜索结果生成类型化 observation，但 capability
  仍为 unavailable，不能据此批准或写入。
- 后端增加图片上传/复核 observation 与 receipt 的严格操作类型校验。图片上传
  必须以 `picture_child_verified` 为完成证据；复核的子记录核验语义调整为
  “图片子记录集合和字段指纹保持不变”。
- `260061860` 实机只读预检已证明实库不存在 `SampleNo` 单列唯一约束；
  `SpecialWoolCheckUI` 功能权限、可能存在的 `btnCheck` 控件权限和文件服务器
  ACL 仍需 Windows 实机只读证明，真实上传和复核继续禁用。

### 6.2 打印交互边界

- 用户可选“打印”或“暂不打印”；两种选择都绑定当前制品 SHA-256。
- 网页无法可靠驱动用户本地 Excel 打印，因此“打印”会先提供下载/打开，
  只有用户勾选“已在 Excel 中完成打印”后才记录 `printed=true`。单纯打开
  或下载不会被误记为已打印；选择“暂不打印”则直接继续。

## 7. `260061860` 真实生成验收

- 共享盘命中 4 个顶层目录：`260061860`、`260061860-1`、`260061860-2`、
  `260061860-z`，索引共返回 376 张可选图片。
- 只读任务快照中完整命中 1 个项目：`纤维微观形貌 + GB/T 36422-2018`；样品
  名称拆为 2 个业务候选，样品识别拆为“横截面/纵面”两项，`give_judgement=0`。
- 实际选用 `260061860-1/2_q36.bmp` 生成 `.xls`。单图保存后位置为
  `x=3000, y=0, width=15589, height=11698`（单位 1/100 mm），画布为
  `21600 × 11700`，即贴满高度并横向居中。
- 保存后重读确认默认打印区域为 `$A$1:$L$37`；LibreOffice 渲染的“微观形貌”
  页为单页，样品编号、名称、图片和空白判定栏位置符合模板。
- 旧系统真实写入未发生。第一次预检联调曾暴露流程能力快照缺少
  `external_write`；现已补充兼容迁移和回归测试，该能力仅允许建立禁写预检单。

## 8. `26A029794` 既有选图验收事实

- 共享盘命中两个顶层文件夹：`26A029794-lisy`、`26A029794-补拍`。
- 第一组 11 张 BMP，第二组 37 张 BMP；合计 48 张。
- 同编号索引总计 144 个文件：48 BMP、48 SIF、48 TXT。
- 旧系统任务单中两个 `GB/T 36422-2018` 项目名称均为 `膜平面形貌`，因此按
  已确认的同标准子项规则构成完整匹配。
- 网页实测目标流程排到目录首位；两个文件夹可同时选择，48 张图片合并展示；
  实测选择 2 张、打开大图、前后切换并提交，运行进入“已完成”。
- 验收前后 144 个索引文件均存在，大小和 mtime 指纹不一致数为 0；未写共享盘，
  未写旧检务系统。

## 9. 验证记录

- 本轮电镜完整流程、生成器、外部操作和引擎联合回归：71 项及 2 个子测试通过；
  此前相关目录/持久化回归 84 项通过。
- 真实容器完成 `.xls` 的 UNO 写入、关闭、重开、打印区域和图片边界核对；
  前端专用人工任务 10 项、外部操作面板 6 项通过，生产构建通过。
- FibreCheck 只读探针：24 项通过；任务快照 Bridge：9 项通过。
- C# Writer 尚未在 Windows x86/.NET Framework + FibreCheck 环境构建本轮新增
  操作；其实现当前明确拒绝图片上传和复核类型，不存在误入根数法写入路径的回退。

### 9.1 2026-08-05 图片比例与契约回归

- 容器中真实 LibreOffice/UNO 重新生成、关闭和重开 `.xls`：21/21 项通过，
  真实集成用例覆盖非方形单图及 2、5、10 图。
- Parallels Windows 11 + 桌面版 Microsoft Excel 只读 COM 验收：单图宽高比
  `1.3337`，2/5/10 图均通过比例、边界、不重叠、唯一 `Print_Area`
  和验证前后文件不变检查；验收后 Excel 残留进程为 0。
- 后端执行引擎、电镜流程、外部操作、Bridge 和定义校验联合回归
  113 项通过；只读探针与任务快照 Bridge 38 项通过；前端人工任务
  12 项及生产构建通过。

### 9.2 2026-08-05 旧系统实库只读预检

- Parallels Windows 11 中的 x64 探针已在 Oracle 只读事务中运行
  `--special-wool-image-dry-run`，`write_performed=false`、`ready_for_write=false`。
- `260061860` 的任务项目唯一命中“纤维微观形貌 / GB/T 36422-2018”，
  脱敏 `task_check_item_id/check_item_id` 与流程选择契约一致。
- 该编号在旧库已有 2 条精确同号主记录；编号族还占用了 `-2`至 `-6`，
  且探针确认没有 `SampleNo` 单列唯一索引。因此不能把“先查询再选号”
  当作多人并发的唯一保障。
- 现有两条同号主记录都是“细度 / 定量试验”，未读到图片子记录；
  这仅证明当前号码的实际状态，不替代一条既有图片记录的主子表回读样本。

## 10. 下一步

1. 将只读任务快照 Bridge 配置为部门 Windows 常驻进程，而不是依赖开发期单次运行；
2. 在 Windows 实机找到一条已有图片主/子记录做只读回读，并证明文件服务器
   路径语义、ACL 及上传账号权限；
3. 实现 Bridge 侧全局串行的远端编号重查与保存后精确回读，然后证明特纤
   复核的功能/控件权限和主/子记录不变性；
4. 优化运行完成页对图片结果和生成制品的专用展示；
5. 用“无文件夹匹配”和“任务单已修改”编号验证降级与缓存刷新体验。
