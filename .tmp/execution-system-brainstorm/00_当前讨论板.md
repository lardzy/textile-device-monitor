# 00. 当前讨论板

> 最后更新：2026-08-19
> 用途：新环境中的 Codex 应先阅读本页，再按链接进入详细资料。

## 当前阶段

**2026-08-19 电镜原始记录日期格式修复：模板 K2 的 TODAY() 日期使用随
系统区域渲染的 BIFF 内建格式，经 LibreOffice 往返保存被固化为 en-US
显式格式（`[$-409]m/d/yyyy`，美国顺序）；现于生成时经 UNO 显式改写为
`YYYY"年"M"月"D"日"`，渲染与查看环境区域无关，TODAY 公式语义保留。
写入器重开文件后按显示串行为验证，调用方强制检查；worker 容器内真实
LibreOffice 端到端验证通过，后端全量 587 项通过。详见 CHANGELOG。**

**2026-08-19 图片放置崩溃恢复体验修复（已提交 a16bb05，现场实测待做）：
预留占位改写自描述标记，崩溃残留可被后续尝试识别接管（不再产生幻影
人工冲突确认）；冲突分支统一现状分析——完整批次直接收尾、部分落盘
断点续放、仅外来冲突转人工；覆盖授权不匹配撤销后同样先走内容识别。
新增 6 项测试，专项 31 项、后端全量 586 项通过。详见 CHANGELOG
2026-08-19 条目。**

**2026-08-18 P1/P2 可靠性修复完成：检验记录生成器升级至 v4，精确保留
字母、前导零和超长编号；报告图片放置绑定源文件指纹，采用整批暂存、
原子替换和可恢复回滚，新出现的同名冲突必须重新人工确认；人工接口只
记录决定，CIFS 写入由 Worker 在释放数据库事务后执行；报告图片共享根
纳入可写性与就绪检查。执行系统专项 169 项及 64 个子测试通过；后端全量
599 项通过、4 项跳过，另有 1 项因本机缺少 UNO 失败；前端 Node 18 项与
Vitest 120 项通过。真实 CIFS/UNO/PostgreSQL 运行态仍待现场验收。**

**2026-08-18 字母检验编号兼容修复 + 电镜流程恢复打印确认：登记工作簿
AS4 对含字母编号（如 26A045793）不再数值化崩溃（纯数字仍写数字格，
字母/前导零写文本格，与模板占位类型一致；全后端与 C# Writer 排查仅此
一处数值转换）。两条电镜流程在生成原始记录后恢复“确认是否打印”节点
（默认打印机 + 默认打印区域，前端打印指引 UI 复用），微观形貌发布 v6、
横截面 v4（昨日无打印时代校验和经 `legacy_no_print_contract` 保留，
存量库自动升级）。验证结论已合并至上方本轮可靠性修复记录。**

**2026-08-18 电镜两条流程末尾新增“放置报告上传图片”节点：选图按
`{编号}-{样品识别}[-序号]` 写入 `\\192.168.105.82\公共交换文件\数据分析中心\
3-报告上传图片\8-材料检测中心\1-微观形貌-GB T 36422\{编号}\`；无冲突零人工
自动放置，同名冲突暂停询问覆盖/取消（可复制完整路径）。新可写 CIFS 卷
`report_images_cifs` + 独立凭据变量 `SMB_USER_C/SMB_PASS_C`（该共享拒绝匿名
挂载）；存量流程启动自动升级（微观形貌 v5、横截面 v3）。后端/前端测试与
真实共享盘冒烟通过，待用户完整流程实测。详见
`15_报告上传图片放置节点.md`。**

**2026-08-15 生产部署准备完成：Windows Bridge 安装包
`textile-execution-bridge-setup-1.0.0.exe` 已在 VM 构建并取回
（SHA256 0c47d768…，Writer 钉值核验一致，快照桥含套餐标题行修复）；
生产开放范围确认为 5 个工作流（再生纤×2、电镜微观形貌、电镜横截面、
纸浆定性）；`production-bootstrap.sh` 改白名单收敛、新增
`production.env.example` 模板、compose 清理受控覆盖残留项、
`12_生产部署方案.md` 更新至迁移 0007。可随时开始生产机部署。**

**2026-08-15 受控测试覆盖通道移除：电镜流程运行输入中的"受控测试覆盖"
字段（会被运行准备页渲染给检验员）已删除，1→2 追加登记只走正式人工确认
通道（existing_record_decision）；后端门禁/回执/设置项同步移除，微观形貌
（v4）与横截面（v2）已自动升级发布。Writer/Bridge 侧休眠代码并入下次
编译清理。**

**2026-08-15 项目匹配规则配置化落地：编号 → 项目类型的匹配规则
（项目名别名、测试方法、目录策略、结果探针）迁移至 `execution_project_rules`
表，管理员在流程管理中实时编辑、保存即生效（profile 缓存按 revision 自动
失效）；新增通用节点 `core.project_match` 与节点 `match_rule` 参数，为
共性节点参数化与可视化配置铺路。写门禁经规则表反查，模板绑定与写入契约
仍代码钉死。详见 `14_项目匹配规则配置化.md`。**

**2026-08-15 新增电镜—纤维横截面 GB/T 36422-2018 流程并通过 260191285
真实链路首验（上传 260191285-1、复核、登记 1→2 并校对，既有手工登记未动）。
与微观形貌同链，经 `microscopy_families` 家族注册表切换任务项目匹配
（5103.426 / 纤维横截面）、原始记录 A1 标题（纤维横截面原始记录）和
旧系统登记模板族（纤维横截面 1/2/3 张图三个模板，探针签发指纹）。
首验同步修复写桥项目门禁与封顶失败操作的复活路径。
详见 `13_电镜纤维横截面首版.md`。**

**微观形貌与纸类两条线均已完成真实写入验收。2026-08-07 修复两处
原始记录质量回归并完成重传：容器挂载 SimSun 后原始记录字体与模板逐格
一致；图片水平锚点因子改为 1.0、图间间距改为 0（紧密排列），
1/2/3/5/6/7/10 图均通过 Windows Excel COM 实测与 PDF 截图核对。
`260111037` 已按修正版重新上传 `260111037-1` 并完成特纤复核；检验记录
登记保留既有 2 条已校对记录不动。排版约束的可复用登记格式见
`11_原始记录排版约束格式.md`。**

首次写入的误判插曲（Bridge 因控制台编码误判成功为失败）已按人工对账处置，
详见 `CHANGELOG.md` 2026-08-01 条目。Runner 输出编码已修复。

## 当前已经实现

1. 执行系统独立登录、角色、用户凭据槽位、工作流设计器和不可变版本；
2. PostgreSQL 持久化 DAG、租约 Worker、人工任务、条件/并行/汇合、SSE、
   Outbox、重启恢复和节点重试；
3. 文件根别名、后台索引、受控工作副本、核对和发布；
4. 再生纤根数法和面积法文件节点、结果读取、多文件选择与主单；
5. 按工作簿真实文件头识别 OLE/OOXML，兼容扩展名被直接改写的文件；
6. 根数法和面积法结果读取均输出 `I8` 检验员；
7. 旧系统根数法上传节点、外部操作/attempt、Bridge 领取与 Writer 已实现；
8. 外部操作绑定凭据 revision、账号作用域、样品业务键、源文件
   fingerprint/SHA-256/I8 和有效期；
9. 用户批准会把任务暴露给已启用的 Windows Bridge；批准界面必须按真实副作用
   提示，不能再描述为“只保存本地状态”；
10. 当前安全修复在 Writer 复制前增加服务端持久化许可，取消或许可失败时不得
    创建文件；真实写入通过保持 `EXECUTION_BRIDGE_ENABLED=false` 冻结，已有令牌保留；
11. 待对账操作只允许管理员以专用权限和 CSRF 提交“完整写入”或“完全未写入”
    两种终局声明；部分成功、计数/哈希不一致或证据过期继续保持业务围栏。
12. 电镜专用流程使用后台递归索引、旧系统任务快照缓存和独立只读 Bridge；
    支持多文件夹合并、1～10 张图片选择、大图及前后切换。
13. 微观形貌原始记录节点使用版本化 `.xls` 模板，支持样品名称候选、样品识别、
    按需判定和 1～10 图等比例布局；保存后逐图重读几何、重叠和唯一打印区域，
    并已修正 Windows Excel 对 BIFF8 水平锚点的解释差异。
14. 图片上传与特纤复核采用独立外部操作协议，上传预检绑定脱敏任务项目；
    `260111037-1` 已完成真实上传和复核。上传文件名统一为“最终分配编号 +
    原模板名”，卡片和最终批准弹窗均显示同一目标文件名。
15. 纸类 GB/T 4688-2020 定性流程已形成完整 DAG：读取 `Sheet1!W32` →
    人工单选 → 特种毛定性上传 → 特纤复核 → 检验记录登记（只保存、不校对）。
    `26W006701` 与 `26W006687` 的共享盘读取已经通过，旧系统写入等待 Windows
    Bridge 完成任务单刷新和 Writer 实机验证。
16. 生产试运行部署方案已定稿（`12_生产部署方案.md`，HTTP-only，仅开放 4 个
    流程）；Windows Bridge 全部物料集中在
    `textile-device-monitor/packaging/windows-bridge/` 打成单一 Inno Setup
    安装包，机密不进包。

当前数据库迁移头为：

```text
0006_task_snapshot_cache
```

当前默认发布的再生纤根数法流程尚未自动接入旧系统上传节点，避免 Bridge 不存在
时影响现有查找、读取和选择流程。

## 多人、多账号的确定方向

不在每位用户电脑上安装 FibreCheck 桌面自动化 Agent。采用：

```text
多人网页提交
  -> PostgreSQL 持久队列
  -> 一台集中式 Windows Bridge
  -> 每任务一个短生命周期 x86 .NET Framework Runner
  -> 使用该任务绑定的旧账号无界面登录并调用官方 DAL
```

- Bridge 可以先运行在当前 Parallels Windows 11，生产时再迁移到部门常驻
  Windows 主机；
- 不依赖 WPF 窗口焦点、交互桌面或每人安装；
- 用户提交可并发，实际旧系统写入首版全局并发固定为 1；
- 稳定验证后才考虑提高到 2～4；
- 同账号、完整样品号、前 9 位样品家族和目标文件始终串行；
- 每个 Runner 结束立即退出，隔离 `CurrentLoginedStaff`、ODP.NET 和静态状态；
- 远端副作用开始后结果未知时进入 `reconciliation_required`，绝不盲目重试。

完整、可随 Git 带到 Windows 的交接手册：

```text
textile-device-monitor/docs/legacy-fibrecheck-bridge-handoff.md
```

## Windows 11 下一步

1. ~~单独复制被 Git 忽略的 `.tmp/FibreCheck/` 和必要取证资料到 Windows~~（完成）；
2. ~~阅读 `docs/legacy-fibrecheck-bridge-handoff.md`~~（完成）；
3. ~~运行 `tools/legacy_fibrecheck_probe/` 的测试和 manifest 模式~~（11 项通过）；
4. ~~对 `260187115` 运行只读 Oracle 对账~~（完成）；
5. ~~确认现有记录、人员映射、附件和报告更新情况~~（完成）；
6. ~~创建 x86 .NET Framework 无界面只读登录 Runner~~（完成）；
7. ~~验证两个账号进程隔离~~（lisy/fuzhu1 双真实账号并行无串号）；
8. ~~Runner dry-run 与 source/target 编号拆分~~（完成）；
9. ~~Bridge 外壳与写入实现~~（完成，见下）；**`260187115-1` 首次受控写入
   已于 2026-08-01 完成并通过写后核验**；
10. ~~许可握手/取消/账号、PostgreSQL 并发门禁和 `reconciliation_required`
    管理员人工解决端点~~（完成）；后续先做故障注入、机器生成的只读观察产物
    绑定和凭据 ACL 收口；门禁通过后才允许第二次端到端真实写入，最后再考虑
    上传节点接入默认流程的配置化开关。

## 阶段 D 关键环境事实（2026-07-31 核验）

- 官方 DAL 链路要求 `FibreCheckWriter.exe.config` 带
  `<add name="bool" value="edmmapping number(1,0)"/>`（旧客户端
  `FibreCheck.exe.config` 同款），否则 ODP.NET 11.2.0.2 报 MappingException 2019；
- 32 位 ODAC 11.2.0.2.50 xcopy 包来自 `\\192.168.105.66\software\2-业务系统\ODAC`，
  本机复制到 `.tmp/odac32/`；本机 GAC_32 已有 4.112.2.50；
- 用户现行客户端：`C:\Users\lishuyang\Downloads\FibreCheck`；
- 演练后端：原生 uvicorn + worker + SQLite（便携 PG 因外网速度不足未下载，
  偏离已记录）；`.env` 在 `textile-device-monitor/backend/.env`（git 忽略）；
- 中文 Windows 控制台默认 GBK：任何解析工具进程 stdout 的消费方必须注意
  编码（Runner 已强制 UTF-8 输出）。

注意：要表达“读取 260187115 的文件、上传为 260187115-1”，工作流需要把当前
单一编号拆为：

```text
source_inspection_number
target_sample_number
```

~~这仍是首次写入前的必做项。~~ 已于 2026-07-31 落地：运行创建支持可选
`target_sample_number`（缺省回退检验编号），预检/批准围栏统一绑定目标编号；
严格 `additionalProperties:false` 的流程需在 `input_schema` 声明该属性。

## Windows 环境网络与连接事实（2026-07-30 核验）

- 本 VM 为 Parallels 共享网络（10.211.55.x），经 Mac NAT 访问内网；
- 共享盘 `192.168.105.82:445` 可达；主配置 Oracle 地址 `10.1.30.146:1521`
  连接超时不可达；
- 番禺检务库备用地址 `192.168.105.106/orcl` 可达，数据库为 Oracle 11g
  11.2.0.1.0，必须使用 Thick 模式（x64 Python + Instant Client 19.31，
  存放于 `.tmp/oracle-ic/`）；
- 主配置 `FibreCheckEntities` 账号在当前环境 ORA-01017 被拒；只读对账使用
  `WebService.dll.config` 的 `PanYuJianWu` 条目凭据（探针
  `--credential-profile` 参数，凭据不出配置文件）；
- 探针还支持 `--data-source` 覆盖备用地址；二者均不影响只读边界。

## Windows 环境材料边界

Git 不包含旧安装包或真实凭据。当前 Mac 本地材料：

```text
.tmp/FibreCheck/
.tmp/fibrecheck_tools/
.tmp/execution-system-secrets/inspection-systems.env
```

它们不得使用 `git add -f`。若 Windows 使用独立克隆，需要单独受控复制。

## 最新验证

- **2026-08-07 原始记录字体与图片画布回归修复**：容器缺宋体导致
  LibreOffice 重存替换字体（DejaVu/Arial）且旧水平锚点补偿 1.2745 失效
  放大图片；挂载 SimSun、`MICROSCOPY_BIFF_EXCEL_X_SCALE=1.0`、
  `IMAGE_GAP=0` 后，1/2/3/5/6/7/10 图制品经 Windows Excel COM 实测
  （右缘 ≤21.6 cm、下缘 ≤11.7 cm、比例保持、无外加间距）与 PDF 截图
  目视核对全部通过。注意：测试套件结束时会清空其目标库，运行库
  `textile_execution_local_test` 曾被误清，已由迁移+引导自动重建；
  测试现专用 `textile_execution_pytest_test`。
  详见 `09_电镜纤维微观形貌首版.md` 9.3 与 `11_原始记录排版约束格式.md`。

- **2026-08-06 微观形貌与纸类全链路**：`260111037` 检验记录登记新增并校对
  （登记补丁改为 BIFF 就地改写，仅写必填格，字体公式保留；OLE 容器布局
  原位保留通过旧系统 NPOI 1.2.3 校验）；纸类 `26W006701`（单位空）、
  `26W006687`（单位 `%`）完成上传、复核与通用登记保存；纸类中文 JSON 包
  解析修复（PowerShell 5.1 无 BOM UTF-8）。

- **2026-08-05 纸类定性流程与最终文件名契约**：真实共享盘索引已就绪；
  `26W006701` 的 `Sheet1!W32` 为“草浆、木浆、竹浆”、单位为空，
  `26W006687` 为“木浆 100”、单位为 `%`。工作流已发布为
  读取—选择—上传—复核—登记—结束七节点链。后端专项 85 项和 47 个子用例、
  Bridge 27 项、前端目标测试 11 项及生产构建通过；backend、worker、frontend
  已在当前 Docker 环境健康运行。两条任务单快照仍为 queued，未执行纸类真实写入。

- **2026-08-05 微观形貌比例与末端契约**：定位 LibreOffice/Excel 对
  BIFF8 水平锚点的单位差异，加入模板版本化补偿；单图与 2/5/10 图在
  容器 UNO 和 Windows Excel 中均通过比例、边界、不重叠和唯一打印区域
  验收。任务快照升为 v3，打印完成不再被打开/下载误判；后端联合
  113 项、只读探针/Bridge 38 项、前端 12 项及生产构建通过。

- **2026-08-03 电镜纤维微观形貌首版**：真实编号 `26A029794` 命中两个目录、
  48 张 BMP；旧系统只读快照命中同一标准下的 `膜平面形貌` 子项，四项业务
  条件完整匹配。网页完成多目录、选 2 张、大图前后切换和提交；144 个索引
  文件验收前后指纹差异为 0。详见 `09_电镜纤维微观形貌首版.md`；

- **2026-08-01 待对账人工处置闭环**：新增管理员专用权限、只读上下文和
  CSRF 写端点，只允许“完整写入”或“完全未写入”两种终局结论；显式绑定最新
  failed post-boundary attempt、payload checksum、目标样品与文件 SHA-256，
  相同请求可幂等重放。取消或兄弟失败发生在待对账前后时，运行均保持
  `cancel_pending/failure_pending`，直到 Bridge 回报或管理员结论收敛；后端
  Bridge/外部操作/引擎/API 联合回归 91 项全过，前端 Node 18 项 + Vitest
  79 项全过并完成生产构建。当前材料标记为 `admin_attestation_v1`，不是机器
  签名探针产物；
- **2026-08-01 Windows ARM64 安全复查与 PostgreSQL 门禁**：整体方向不变，
  第二次真实写入暂停；已补副作用许可握手、账号匹配、单调阶段、全局领取
  容量和严格完成核验；后端 Bridge/外部操作 42 项、引擎/API 35 项及 2 个
  子测试、Bridge 控制器 5 项均通过；前端完整 `npm test` 为 Node 18 项 +
  Vitest 71 项全过，x86 Writer 重新编译通过；一次性 PostgreSQL 15.18
  环境中的并发模块 6 项全过，其中包含 Bridge 全局容量事务锁；测试环境已
  停止并清理；
- **Windows `.xlsx` 原子替换修复**：真实 PostgreSQL 测试暴露 openpyxl
  只读校验在 Windows 上残留文件句柄的问题；改为显式持有并关闭底层二进制
  流，文件变更完整套件 23 项全过；
- **`260187115-1` 首次受控写入完成并核验通过（2026-08-01）**：DB 字段、
  检验员/创建人散列、物理文件 SHA-256 与源文件逐项一致；
- Bridge 后端：17 项 Bridge 测试 + 19 项外部操作回归全过（迁移头 0005）；
- Writer：只读探针与两项负面排练通过；端到端真实写入一次成功；
- 阶段 A Runner：FakeDb 自测 10 组场景通过；`lisy`/`fuzhu1` 双真实账号
  只读登录均 exit=0 且并行无串号；
- `260187115-1` dry-run 实测 exit=0：目标编号远端精确/包含计数均为 0、
  源记录存在、最终清单字段与真实 `260187115` 记录逐项一致；
- 文件链路实物核验：`\\192.168.105.82\fibrecheckfile$` 上传目录中
  `260187115` 文件存在且唯一，与 DB `FilePath` 一致；
- `260187115` 只读 Oracle 对账完成（Thick 模式，只读事务，全程 rollback）；
- 探针 `--data-source` / `--credential-profile` 扩展与脱敏修复后 11 项测试通过。

## 继续阅读

- `04_技术方向与可行性.md`：总体架构及 Windows 能力边界；
- `06_待决策问题与决策日志.md`：D-011～D-021；
- `07_首版实现与验收记录.md`：当前实现和静态逆向证据；
- `08_再生纤文件节点首版.md`：两种方法的匹配和结果读取；
- `09_电镜纤维微观形貌首版.md`：电镜任务匹配、缓存、选图和真实验收；
- `10_纸纤维鉴别分析首版.md`：纸类 GB/T 4688-2020 流程、W32 结果和待验收项；
- `11_原始记录排版约束格式.md`：模板填格/插图项目的约束登记格式与已登记项目；
- `13_电镜纤维横截面首版.md`：横截面同链变体的家族注册表与模板指纹；
- `14_项目匹配规则配置化.md`：编号→项目类型匹配规则的实时维护；
- `15_报告上传图片放置节点.md`：电镜流程末尾图片放置、命名与冲突询问；
- `CHANGELOG.md`：每轮变更摘要。

## 每轮更新规则

1. 新事实先写入本页或对应主题；
2. 明确结论同步到 `06_待决策问题与决策日志.md`；
3. 技术方向变化同步到 `04_技术方向与可行性.md`；
4. 代码和测试事实同步到 `07_首版实现与验收记录.md`；
5. 每轮结束追加 `CHANGELOG.md`；
6. 凭据、连接串、对账结果和旧 DLL 永不写入这些 Markdown 或 Git。
